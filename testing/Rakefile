require "rake/clean"

thisdir = File.expand_path File.dirname __FILE__ 

require [thisdir, "compile_gtest"].join "/"

CLEAN.include("all.test")

task :test_all do |t|
  compile_gtest(["gtest/all.cu"])
  run(GTEST_NAME)
end
