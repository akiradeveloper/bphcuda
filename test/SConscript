Import('env')

# clone the parent's env so that we do not modify it
my_env = env.Clone()

LIBS = ''
common_libs = ['pthread', 'gtest']
my_env.Append(LIBS = common_libs)

my_env.Program('bphcuda', 'bphcuda/main.cpp', LIBS)
my_env.Program('thrusting', 'thrusting/main.cpp', LIBS)
